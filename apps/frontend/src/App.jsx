import Routing from "./Routing"

function App() {

 {/* React.useEffect(() => {
    AOS.init({
      offset: 100,
      duration: 800,
      easing: "ease-in-sine",
      delay: 100,
    });
    AOS.refresh();
  }, []); */}

  return (
    <>
      <Routing />
    </>
  )
}

export default App;
